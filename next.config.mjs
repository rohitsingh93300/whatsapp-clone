/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        remotePatterns:[
            {hostname: "tough-dinosaur-160.convex.cloud"}
        ]
    }
};

export default nextConfig;
